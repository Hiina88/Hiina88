# Prompt Genius Pocket

スマホからAIプロンプトをすぐ探してコピーできる、依存関係なしの静的PWAです。

## 起動方法

```bash
npm start
```

デフォルトでは `0.0.0.0:4173` で起動します。ホスティング環境が `PORT` を指定する場合は、その値を自動で利用します。

## 開発チェック

```bash
npm run check
```
